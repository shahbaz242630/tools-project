node_modules
dist
.next
coverage
generated
pnpm-lock.yaml
docs

# Regenerated by `next typegen` on every typecheck, always with double quotes,
# and the file itself says not to edit it. Formatting it means the working tree
# is dirty after every `pnpm typecheck`, and CI only passes today because
# format:check happens to run before typecheck. Swap those two steps and main
# goes red for a reason nobody would connect to the change that did it.
apps/web/next-env.d.ts